# Multi-purpose, customizable, manually triggered Crowbar job
- project:
    name: openstack-crowbar
    crowbar_job: '{name}'
    concurrent: True
    triggers:
    cloud_env: ''
    reserve_env: false
    cleanup: never
    ses_enabled: false
    jobs:
        - '{crowbar_job}'
