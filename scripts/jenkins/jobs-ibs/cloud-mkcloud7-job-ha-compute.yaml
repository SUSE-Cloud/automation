- job:
    name: 'cloud-mkcloud7-job-ha-compute'
    node: cloud-trigger
    disbaled: true

    triggers:
      - timed: 'H 21 * * *'

    logrotate:
      numToKeep: 7
      daysToKeep: -1

    builders:
      - trigger-builds:
        - project: openstack-mkcloud
          condition: SUCCESS
          block: true
          current-parameters: true
          predefined-parameters: |
            TESTHEAD=1
            cloudsource=develcloud7
            nodenumber=4
            hacloud=1
            mkcloudtarget=all_batch
            scenario=cloud6-4nodes-compute-ha.yml
            want_node_aliases=controller=2,compute=2
