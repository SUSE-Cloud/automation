- job:
    name: 'cloud-mkcloud6-job-raid'
    node: cloud-trigger

    triggers:
      - timed: 'H 20 * * *'

    logrotate:
      numToKeep: 7
      daysToKeep: -1

    builders:
      - trigger-builds:
        - project: openstack-mkcloud
          condition: SUCCESS
          block: true
          current-parameters: true
          predefined-parameters: |
            TESTHEAD=1
            cloudsource=develcloud6
            nodenumber=2
            controller_raid_volumes=2
            want_raidtype=raid1
            mkcloudtarget=all
