- job-template:
    name: 'cloud-mkcloud{version}-job-ha-linuxbridge-{arch}'
    node: cloud-trigger

    triggers:
      - timed: 'H 21 * * *'

    logrotate:
      numToKeep: 7
      daysToKeep: -1

    builders:
      - trigger-builds:
        - project: openstack-mkcloud
          condition: SUCCESS
          block: true
          current-parameters: true
          predefined-parameters: |
            TESTHEAD=1
            cloudsource=develcloud{version}
            nodenumber=4
            clusterconfig=services='data+network+services=2'
            want_ceph=0
            hacloud=1
            want_node_os=suse-12.2=2,suse-12.1=2
            mkcloudtarget=all_noreboot
            networkingplugin=linuxbridge
            networkingmode=vxlan
            want_node_aliases=controller=2,compute=2
            label={label}
