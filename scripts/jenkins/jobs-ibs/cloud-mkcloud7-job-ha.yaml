- job:
    name: 'cloud-mkcloud7-job-ha'
    node: cloud-trigger
    disbaled: true

    triggers:
      - timed: '11 2 * * *'

    logrotate:
      numToKeep: 7
      daysToKeep: -1

    builders:
      - trigger-builds:
        - project: openstack-mkcloud
          condition: SUCCESS
          block: true
          current-parameters: true
          predefined-parameters: |
            TESTHEAD=1
            cloudsource=develcloud7
            nodenumber=4
            networkingmode=vxlan
            hacloud=1
            mkcloudtarget=all_noreboot
